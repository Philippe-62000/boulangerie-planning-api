import React from 'react';
import CommandeTGT from './CommandeTGT';

/** Commande fournisseur Mill'Ange (même logique que TGT, catalogue séparé). */
const CommandeMillAnge = () => <CommandeTGT channelKey="MILLANGE" />;

export default CommandeMillAnge;
